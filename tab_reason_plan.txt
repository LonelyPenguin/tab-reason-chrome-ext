put most recent tab opened id code in background.js when a new tab is opened
in popup.js, get most recent tab id, use it as key in chrome.storage with popup input as value
in background.js, listen for when a new tab is switched to active, then change the popup tooltip to the value of the current tab id